# openentropy-wasm

WebAssembly bindings for OpenEntropy.

Uses browser-side signals (timing jitter, scheduling noise, and `crypto.getRandomValues`) for entropy collection in web contexts.

## Install

```toml
[dependencies]
openentropy-wasm = "0.7"
```

## Repository

https://github.com/amenti-labs/openentropy
