---
title: 'GPU Sources'
description: 'Entropy from GPU execution and memory-domain crossing behavior'
---

GPU sources use compute scheduling and memory coherence variability across GPU
subsystems.

## Sources

- `gpu_divergence` — shader divergence timing
- `iosurface_crossing` — CPU/GPU memory-domain crossing timing
- `nl_inference_timing` — framework inference timing via accelerator paths
