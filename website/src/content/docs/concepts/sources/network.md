---
title: 'Network Sources'
description: 'Entropy from network path timing and RF environment variability'
---

Network sources use latency jitter and radio-environment variability.

## Sources

- `dns_timing` — DNS resolution timing variance
- `tcp_connect_timing` — TCP handshake latency variability
- `wifi_rssi` — ambient WiFi RSSI noise floor changes
